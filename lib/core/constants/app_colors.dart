import 'package:flutter/material.dart';

class AppColors {
  // Prevent instantiation
  const AppColors._();

  static const Color scaffoldBackground = Color(0xFFFDFAF6);
  static const Color textDark = Color(0xFF1E3028);
  static const Color textGrey = Color(0xFF757575);           // Muted grey
  static const Color primary = Color(0xFF4C8C74);            // Sage Green
  static const Color primaryDark = Color(0xFF1E4030);
  static const Color accentPurple = Color(0xFF8B7FFF);
  static const Color accent = Color(0xFFD4AF37);
}